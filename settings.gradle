rootProject.name = 'banklink'

include 'core', 'link', 'authentication', 'payment' 

rootProject.children.each { it ->
    it.name = 'banklink-' + it.name
}

private nameChildren(parent) {
    parent.children.each {
        it.name=it.parent.name+"-"+it.name
        nameChildren(it)
    }
}